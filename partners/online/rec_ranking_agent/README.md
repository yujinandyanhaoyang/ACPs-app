# Deprecated Partner Material

`partners/online/rec_ranking_agent/` is deprecated.

- It is retained temporarily for backward compatibility and historical ACPs registration evidence.
- It should not be used in new service startup flows.
- Runtime migration target: `recommendation_engine_agent` (see `UPGRADE_PLAN.md`).
