fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
# Config-driven dataset path resolution
python-dotenv>=1.0.0
# DashScope embedding API (optional, for high-quality semantic embeddings)
# Without this, will fallback to hash-based embeddings (no API key needed)
requests>=2.28.0
aiohttp>=3.9.0  # Async HTTP for DashScope API
# Core ML libraries (lightweight, no CUDA required)
scikit-learn>=1.5.0
scipy>=1.11.0
networkx>=3.0
# Optional: sentence-transformers for offline embeddings (requires ~7GB, includes PyTorch)
sentence-transformers>=3.0.0
# Testing
python-multipart>=0.0.6
tiktoken>=0.5.0
pytest>=7.0.0
pytest-cov>=4.1.0
httpx>=0.24.0
httpx_sse>=0.4.0
sse-starlette>=2.1.3