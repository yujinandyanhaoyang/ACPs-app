# Legacy Agent Implementations

This `agents/` directory is retained as legacy/reference implementation code.

- It is **not** the ACPs runtime entry point for the upgraded architecture.
- Active ACPs runtime partner materials are under `partners/online/`.
- New development should prioritize the upgrade plan execution paths and partner runtime modules.

Current deprecation note:
- `agents/rec_ranking_agent/` is legacy and will be replaced by the new `recommendation_engine_agent` runtime path defined in `UPGRADE_PLAN.md`.
